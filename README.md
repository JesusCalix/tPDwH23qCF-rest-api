# tPDwH23qCF-rest-api

# Requirements

1. Having UV installed
2. Python 3.13 if not, install it with uv by running `uv python install 3.13`
3. run `uv sync` command
4. Activate environment with `source .venv/bin/activate`


# Suggestions
async could be added as improvement

# Assumptions
no logs because this is not going into prod i decided to skip it

# diagram of the present design and aws design
